export interface ProjectsType {
  image: string;
  link: string;
  title: string;
  subtitle: string;
  description: string;
}

export interface TechsType {
  svg: React.ReactNode;
  text: string;
}

export interface RecomendationsType {
  title: string;
  link: string;
}
